function e(e,t=`Send`){let n=e.audience,r=e.segment?.name??`selected`,i=`Sending starts immediately and can't be undone.`;if(!n)return`${t} “${e.name}” to the current ${r} audience? ${i}`;if(n.sendable===0)return`No one in ${r} can receive this ${e.channel} broadcast — all ${n.total} ${n.total===1?`person is`:`people are`} missing a destination or unsubscribed. Send anyway?`;let a=n.total-n.sendable,o=``;if(a>0){let t=e.channel===`sms`?`phone number`:e.channel===`push`?`push destination`:`email address`,r=[];n.no_destination>0&&r.push(`no ${t}`),n.suppressed>0&&r.push(`unsubscribed or suppressed`),o=` ${a} will be skipped (${r.join(`, `)}).`}return`${t} “${e.name}” to ${n.sendable} of ${n.total} people in ${r}?${o} ${i}`}export{e as t};